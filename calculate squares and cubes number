#include <stdio.h>
int main() 
{
    int num;
    int square, cube;
    printf("Enter a number: ");
    scanf("%d", &num);
    square = num * num;
    cube = num * num * num;
    printf("Square of the number = %d\n", square);
    printf("Cube of the number = %d", cube);
return 0;
}
